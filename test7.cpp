#include<iostream>
#include<string>
#include<cstring>

using namespace std;

int letter[26];

int Add(int index, int n)
{
    return (index + 1) % n;
}

int length(int n)
{
    
}

int main()
{
    string str;
    string pattern;
    int start;
    cin>>str>>pattern>>start;
    int strl = str.size();
    int pl = pattern.size();
    int sign = 0;
    memset(letter,-1,sizeof(letter));
    while(sign<pl)
    {

    }
}

